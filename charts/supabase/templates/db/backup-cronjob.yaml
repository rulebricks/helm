{{/*
The legacy Supabase subchart backup CronJob was retired because subcharts cannot
read the umbrella chart's top-level backup values. The active Barman backup
CronJob lives in the parent chart at templates/db-backup-cronjob.yaml, where it
can consume backup.* and global.storage.* directly.
*/}}

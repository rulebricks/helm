{{- $domain := .Values.global.domain | default "localhost" -}}
{{- $tlsEnabled := eq (include "rulebricks-chart.tlsEnabled" .) "true" -}}
{{- $protocol := ternary "https" "http" $tlsEnabled -}}

Application URL: {{ $protocol }}://{{ $domain }}

{{- if not .Values.ingress.enabled }}
Note: Ingress is disabled. To access the application:

{{- if contains "NodePort" .Values.service.type }}
  export NODE_PORT=$(kubectl get --namespace {{ .Release.Namespace }} -o jsonpath="{.spec.ports[0].nodePort}" services {{ include "rulebricks-chart.fullname" . }})
  export NODE_IP=$(kubectl get nodes --namespace {{ .Release.Namespace }} -o jsonpath="{.items[0].status.addresses[0].address}")
  echo http://$NODE_IP:$NODE_PORT
{{- else if contains "LoadBalancer" .Values.service.type }}
  kubectl get svc --namespace {{ .Release.Namespace }} {{ include "rulebricks-chart.fullname" . }} -w
{{- else if contains "ClusterIP" .Values.service.type }}
  kubectl --namespace {{ .Release.Namespace }} port-forward svc/{{ include "rulebricks-chart.fullname" . }} 8080:80
{{- end }}
{{- end }}

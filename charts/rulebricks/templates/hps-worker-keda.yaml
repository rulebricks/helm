{{- if and .Values.hps .Values.hps.enabled .Values.hps.workers.enabled .Values.hps.workers.keda.enabled }}
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: {{ include "rulebricks-chart.hps-worker.fullname" . }}-scaler
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "rulebricks-chart.labels" . | nindent 4 }}
    app.kubernetes.io/component: hps-worker-scaler
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "rulebricks-chart.hps-worker.fullname" . }}
  minReplicaCount: {{ .Values.hps.workers.keda.minReplicaCount | default 3 }}
  {{- /* Bounded by the solution topic partition count: workers beyond it sit idle. */}}
  maxReplicaCount: {{ .Values.hps.workers.keda.maxReplicaCount | default 48 }}
  pollingInterval: {{ .Values.hps.workers.keda.pollingInterval | default 15 }}  # Check interval in seconds
  cooldownPeriod: {{ .Values.hps.workers.keda.cooldownPeriod | default 300 }}  # Wait before scaling down in seconds
  advanced:
    horizontalPodAutoscalerConfig:
      behavior:
        {{- if .Values.hps.workers.keda.scalingBehavior }}
        {{- toYaml .Values.hps.workers.keda.scalingBehavior | nindent 8 }}
        {{- else }}
        {{- /* Rule traffic is bursty (idle -> firehose). Scale-up is
             exponential: act immediately (no stabilization) and double the
             fleet (or add 8 pods, whichever is greater) every 15s, so
             min -> max takes ~3-4 doubling periods. Scale-down is smooth: wait
             out a 5-minute quiet window, then shed at most 25% of replicas per
             minute so a brief lull between bursts never collapses the fleet. */}}
        scaleUp:
          stabilizationWindowSeconds: 0
          selectPolicy: Max
          policies:
            - type: Percent
              value: 100
              periodSeconds: 15
            - type: Pods
              value: 8
              periodSeconds: 15
        scaleDown:
          stabilizationWindowSeconds: 300
          selectPolicy: Max
          policies:
            - type: Percent
              value: 25
              periodSeconds: 60
        {{- end }}
  triggers:
  {{- /* Lag scaling needs the KEDA operator to reach Kafka directly. Under
       GCP-flavored OAUTHBEARER it can't authenticate, so skip the lag trigger
       and let the CPU trigger below drive scaling. */}}
  {{- if not (include "rulebricks-chart.kafka.gatedTokenAuth" .) }}
  # Primary trigger: Kafka lag
  - type: kafka
    metadata:
      # Use templated Kafka bootstrap servers - handles any release name and namespace
      {{- $kafkaBrokers := "" }}
      {{- $externalKafka := false }}
      {{- if and .Values.app .Values.app.logging .Values.app.logging.kafkaBrokers }}
        {{- $kafkaBrokers = .Values.app.logging.kafkaBrokers }}
        {{- $externalKafka = true }}
      {{- else }}
        {{- $kafkaBrokers = (include "rulebricks-chart.kafka.bootstrapServers" .) }}
      {{- end }}
      bootstrapServers: {{ $kafkaBrokers }}
      consumerGroup: generic-workers
      # Must match the topic HPS actually produces/consumes (HPS applies the prefix).
      topic: {{ include "rulebricks-chart.kafka.topicPrefix" . }}solution
      # Lag is in MESSAGES; with chunked bulk dispatch each message is a
      # bounded unit of work, so this approximates seconds of fleet backlog.
      lagThreshold: "{{ .Values.hps.workers.keda.lagThreshold | default 100 }}"
      offsetResetPolicy: latest
    {{- if and $externalKafka (include "rulebricks-chart.kafka.awsIamAuth" .) }}
      {{- /* MSK IAM = KEDA's oauthbearer SASL with the aws_msk_iam token
           provider (there is no `sasl: aws_msk_iam` mode). TLS-only, and KEDA
           has no env fallback for the region. */}}
      {{- $sasl := (((.Values.app).logging).kafkaSasl) | default dict }}
      tls: enable
      sasl: oauthbearer
      saslTokenProvider: aws_msk_iam
      awsRegion: {{ required "app.logging.kafkaSasl.region is required when kafkaSasl.mechanism is aws-iam" $sasl.region }}
    authenticationRef:
      name: {{ include "rulebricks-chart.kafka.triggerAuthName" . }}
    {{- else if include "rulebricks-chart.kafka.kedaSaslMode" . }}
    {{- /* External Kafka with a static SASL credential (Event Hubs, Confluent,
         SCRAM clusters): without tls/sasl + the credential TriggerAuthentication
         the trigger renders but can never authenticate. In-cluster Kafka takes
         the empty branch and renders exactly as before. */}}
      tls: {{ include "rulebricks-chart.kafka.kedaTlsMode" . }}
      sasl: {{ include "rulebricks-chart.kafka.kedaSaslMode" . }}
    authenticationRef:
      name: {{ include "rulebricks-chart.kafka.triggerAuthName" . }}
    {{- end }}
  {{- end }}
  # CPU trigger: the safety net when Kafka lag metrics are unavailable, and the
  # sole scaling signal under GCP-flavored token auth (see note above).
  {{- /* metricType (not metadata.type) is the KEDA >= 2.7 form; the legacy
       metadata 'type' key was removed in KEDA 2.18 and makes the whole
       ScaledObject fail to parse ("KEDAScalerFailed"), silently disabling
       autoscaling. */}}
  - type: cpu
    metricType: Utilization
    metadata:
      value: "{{ .Values.hps.workers.keda.cpuThreshold | default 25 }}"
{{- end }}

{{- $observabilityHost := "" -}}
{{- if and .Values.global.clickstack .Values.global.clickstack.enabled .Values.clickstack.hyperdx.ingress.enabled -}}
{{- $observabilityHost = (.Values.clickstack.hyperdx.ingress.hostname | default (printf "observability.%s" .Values.global.domain)) -}}
{{- end -}}
{{- $valkeyHost := "" -}}
{{- $valkeyAdmin := (((.Values.rulebricks | default dict).cache | default dict).valkeyAdmin) | default dict -}}
{{- $valkeyIngress := $valkeyAdmin.ingress | default dict -}}
{{- if and $valkeyAdmin.enabled (eq ($valkeyAdmin.exposure | default "internal") "ingress") $valkeyIngress.enabled (ne ($valkeyIngress.type | default "ingress") "gateway-api") -}}
{{- $valkeyHost = ($valkeyIngress.hostname | default (printf "valkey.%s" .Values.global.domain)) -}}
{{- end -}}
{{- if and .Values.global.tlsEnabled .Values.global.externalDnsEnabled }}
=============================================================================
RULEBRICKS INSTALLATION - COMPLETE (Automatic DNS)
=============================================================================

External-DNS is managing your DNS records automatically.
Certificates are being provisioned via Let's Encrypt.

DNS records being created:
  - {{ .Values.global.domain }}
  {{- if .Values.supabase.enabled }}
  - supabase.{{ .Values.global.domain }}
  {{- end }}
  {{- if $observabilityHost }}
  - {{ $observabilityHost }}
  {{- end }}
  {{- if $valkeyHost }}
  - {{ $valkeyHost }}
  {{- end }}

Once DNS propagates and certificates are ready:

  {{- if and .Values.global.sso .Values.global.sso.enabled }}
  https://{{ .Values.global.domain }}/auth/signin
  {{- else }}
  https://{{ .Values.global.domain }}/auth/signup
  {{- end }}

To check certificate status:
  kubectl get certificates -n {{ .Release.Namespace }}

=============================================================================
{{- else if .Values.global.tlsEnabled }}
=============================================================================
RULEBRICKS INSTALLATION - 2/2 - COMPLETE
=============================================================================

Welcome to Rulebricks.

{{- if and .Values.global.sso .Values.global.sso.enabled }}
https://{{ .Values.global.domain }}/auth/signin
{{- else }}
https://{{ .Values.global.domain }}/auth/signup
{{- end }}

=============================================================================
{{- else if .Values.global.externalDnsEnabled }}
=============================================================================
RULEBRICKS INSTALLATION - ALMOST THERE
=============================================================================

External-DNS is creating your DNS records automatically.

DNS records being created:
  - {{ .Values.global.domain }}
  {{- if .Values.supabase.enabled }}
  - supabase.{{ .Values.global.domain }}
  {{- end }}
  {{- if $observabilityHost }}
  - {{ $observabilityHost }}
  {{- end }}
  {{- if $valkeyHost }}
  - {{ $valkeyHost }}
  {{- end }}

Since you're using external-dns, you can enable TLS right away:

  helm upgrade {{ .Release.Name }} oci://ghcr.io/rulebricks/helm/stack \
    --namespace {{ .Release.Namespace }} \
    --reuse-values \
    --set global.tlsEnabled=true

TIP: Next time, install with both flags for single-phase setup:
  --set global.externalDnsEnabled=true --set global.tlsEnabled=true

=============================================================================
{{- else }}
=============================================================================
RULEBRICKS INSTALLATION - 1/2 - DNS SETUP REQUIRED
=============================================================================

1. Get your LoadBalancer Address:
   kubectl get svc -n {{ .Release.Namespace }} -l app.kubernetes.io/name=traefik -o jsonpath='{.items[0].status.loadBalancer.ingress[0].hostname}'

2. Configure DNS Records:
   Point the following CNAME record(s) to the LoadBalancer address above:
   - {{ .Values.global.domain }}
   {{- if .Values.supabase.enabled }}
   - supabase.{{ .Values.global.domain }}
   {{- end }}
   {{- if $observabilityHost }}
   - {{ $observabilityHost }}
   {{- end }}
   {{- if $valkeyHost }}
   - {{ $valkeyHost }}
   {{- end }}

3. Enable TLS (Phase 2):
   Run the following command to secure your cluster and finish installation:

   helm upgrade {{ .Release.Name }} oci://ghcr.io/rulebricks/helm/stack \
     --namespace {{ .Release.Namespace }} \
     --reuse-values \
     --set global.tlsEnabled=true

=============================================================================
{{- end }}

{{- /* Advisory only: surface priorityClass/priorityClassName inconsistencies so
       a silent scheduler deadlock ("no PriorityClass ... was found") is caught
       early. Never fail the release here. */ -}}
{{- $pcEnabled := true }}
{{- if and .Values.global .Values.global.priorityClasses (hasKey .Values.global.priorityClasses "enabled") }}
{{- $pcEnabled = .Values.global.priorityClasses.enabled }}
{{- end }}
{{- $criticalNames := list }}
{{- if and .Values.kafka .Values.kafka.controller .Values.kafka.controller.priorityClassName }}
{{- $criticalNames = append $criticalNames (printf "kafka.controller.priorityClassName=%s" .Values.kafka.controller.priorityClassName) }}
{{- end }}
{{- if and .Values.clickhouse .Values.clickhouse.priorityClassName }}
{{- $criticalNames = append $criticalNames (printf "clickhouse.priorityClassName=%s" .Values.clickhouse.priorityClassName) }}
{{- end }}
{{- if and .Values.supabase .Values.supabase.db .Values.supabase.db.priorityClassName }}
{{- $criticalNames = append $criticalNames (printf "supabase.db.priorityClassName=%s" .Values.supabase.db.priorityClassName) }}
{{- end }}
{{- if and (not $pcEnabled) (gt (len $criticalNames) 0) }}

=============================================================================
WARNING: PriorityClass mismatch - critical pods will NOT schedule
=============================================================================

global.priorityClasses.enabled is false, so the release-scoped PriorityClasses
({{ .Release.Name }}-critical / {{ .Release.Name }}-burst) are NOT created, but
these values still reference a critical PriorityClass:
{{- range $criticalNames }}
  - {{ . }}
{{- end }}

The scheduler will reject those pods with "no PriorityClass ... was found" and
the install will hang (kafka/clickhouse/supabase-db never start).

Fix: either set global.priorityClasses.enabled=true, OR clear those
priorityClassName values so the pods run at default priority.
=============================================================================
{{- else if and $pcEnabled (eq (len $criticalNames) 0) }}

=============================================================================
NOTE: Critical infrastructure is running at default scheduling priority
=============================================================================

global.priorityClasses.enabled is true (PriorityClasses were created), but no
critical priorityClassName is set for kafka/clickhouse/supabase-db. These pods
will NOT preempt burst workers to reschedule during an aggressive scale-out.

To enable preemption, set their priorityClassName to "{{ .Release.Name }}-critical".
=============================================================================
{{- end }}
